import { describe, it } from 'vitest'
import type { Post, PostFrontmatter } from '@/types/post'

describe('post types', () => {
  it('compiles imports', () => {
    const _fm: PostFrontmatter | null = null
    const _post: Post | null = null
    void _fm
    void _post
  })
})
